#!/usr/bin/env bash

GCD_BIN="!!GCDLIBEXECBIN!!"
echo "git-cd reset metrics"
echo "This action will reset all the metrics."
read -p "Are you sure? " -n 1 -r
if [[ $REPLY =~ ^[Yy]$ ]]
then
    rm -f ${HOME}/.gcd/gcd.sqlite
    echo
    echo "Metrics have been reset..."
fi

