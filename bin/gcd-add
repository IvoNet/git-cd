#!/usr/bin/env bash

if [[ -z "$1" ]]; then
    echo "Syntax: $(basename $0) alias_name"
    echo "This will add the current folder to the cache"
    exit 0
fi

GCD_BIN="${GCD_DEV_BIN:-!!GCDLIBEXECBIN!!}"
python3 ${GCD_BIN}/gcd.py --add-dir "$(pwd)"
echo "Added: $(pwd)"
