#!/usr/bin/env bash
GCD_DIR="${HOME}/.gcd"
GCD_BIN="${GCD_DEV_BIN:-/usr/local/bin}"
GCD_COMMAND=${GCD_EXEC:-idea .}

source ${GCD_BIN}/gcd $1
if [[ $? -eq 0 ]]; then
    if [[ ! -z "${GCD_COMMAND}" ]]; then
       exec ${GCD_COMMAND}
    fi
fi
